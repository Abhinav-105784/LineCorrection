<UserControl x:Class="LineCorrection.SmoothenLinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
             xmlns:ui="clr-namespace:LineCorrection"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"                       
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="300"
             d:DataContext="{Binding Path=ui.SmoothenLinesViewModel}">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <DockPanel Grid.Row="0" LastChildFill="true" KeyboardNavigation.TabNavigation="Local" Height="30">
      <TextBlock Text="{Binding Heading}" Style="{DynamicResource Esri_TextBlockDockPaneHeader}">
        <TextBlock.ToolTip>
          <WrapPanel Orientation="Vertical" MaxWidth="300">
            <TextBlock Text="{Binding Heading}" TextWrapping="Wrap"/>
          </WrapPanel>
        </TextBlock.ToolTip>
      </TextBlock>
    </DockPanel>
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Label-->
                <RowDefinition Height="Auto"/>
                <!--Combo-->
                <RowDefinition Height="20"/>
                <!--Gap-->
                <RowDefinition Height="Auto"/>
                <!--Angle-->
                <RowDefinition Height="20"/>
                <!--Gap-->
                <RowDefinition Height="Auto"/>
                <!--Buttons-->
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Select Layer" FontWeight="Bold" Margin="0,0,0,5"/>
            <ComboBox Grid.Row="1" x:Name="SelectLayerComboBox" Height="25" Padding="5" SelectionChanged="SelectLineLayers"/>
            <StackPanel Grid.Row="3" HorizontalAlignment="Center">
                <TextBlock Text="Provide Angle" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                <TextBox x:Name="AngleProvided" Width="100" Height="25" Padding="5" TextChanged="AngleProvided_TextChanged"/>
            </StackPanel>
            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Run" Height="30" Margin="5" Click="RunButton_Click" x:Name="Run"/>
                <Button Grid.Column="1" Content="Close" Height="30" Margin="5" Click="CloseButton_Click" x:Name="Close"/>
            </Grid>

        </Grid>
    </Grid>
</UserControl>